version: 2

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - extra-pubkey-algs
        - xmp
        - opentype
        - image-support
        - pkcs11
        - async-http
formats: all
